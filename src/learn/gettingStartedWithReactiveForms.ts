import { delimeterMsg, logF, log } from 'src/learn/utils';

function projectInstallation() {
  log('Hello world');
}

export default function gettingStartedWithForms() {
  delimeterMsg('GETTING STARTED WITH REACTIVE FORMS');
  logF(projectInstallation);
}